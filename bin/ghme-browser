#!/bin/sh
# Quiet browser launcher for ghme/gh-dash.
# xdg-open (via gio) and the browser print to stdout/stderr, which leaks into
# the gh-dash TUI and corrupts its drawing. Discard all output and detach so
# the terminal UI is never touched.
setsid xdg-open "$@" >/dev/null 2>&1 < /dev/null &
